#
# esf.properties
#
# This files lists Studio SDK properties pertaining to the Thread stack.
#
# The version= property should be updated to the release version on a
# release branch, or it should be set to 0.0.0 otherwise. This is the
# version that Studio displays for the loaded stack.
#

id=com.silabs.sdk.stack.super
version=2.2.1.0
dependantSdkVersion=1.0.1
label=Thread SDK
description=Silicon Labs Thread SDK
prop.subLabel=Thread\\ 2.2.1.0

prop.file.appDirectory=app/thread
prop.file.docsFile=documentation/slThread_docContent.xml
