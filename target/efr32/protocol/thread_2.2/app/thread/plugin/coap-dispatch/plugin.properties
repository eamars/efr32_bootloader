name=CoAP Dispatch
description=This plugin dispatches received CoAP requests to different handlers depending on the URI of the request
category=Common
quality=test

providedApis=coap-dispatch
api.coap-dispatch.header=coap-dispatch.h

requiredApis=coap,stack

coap-dispatch.c

implementedCallbacks=emberCoapRequestHandler
